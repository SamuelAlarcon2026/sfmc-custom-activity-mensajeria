## Versión v16

Esta versión corrige el contrato RESTDECISION para devolver `outArguments` como un único objeto dentro de `outArguments[0]`, manteniendo también los campos a nivel raíz para compatibilidad.

# SFMC BITMessage Custom Activity - v15 RESTDECISION doc contract

Versión de diagnóstico/corrección que usa branchResult string: sent / notSent y no reescribe outcomes desde la UI.
